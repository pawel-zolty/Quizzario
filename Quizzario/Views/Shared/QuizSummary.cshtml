@model Quizzario.BusinessLogic.DTOs.QuizDTO

<div class="well">
    <h3>
        <strong>@Model.Title</strong>
        <span class="pull-right label label-primary">@Model.QuizType </span>
    </h3>
</div>
